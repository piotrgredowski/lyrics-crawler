[project]
name = "lyrics-crawler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14.0"
dependencies = [
    "playwright>=1.48.0",
]

[project.scripts]
lyrics-crawler = "lyrics_crawler.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]
